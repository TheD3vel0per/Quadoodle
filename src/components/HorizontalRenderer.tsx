import React from 'react';

class HorizontalRenderer extends React.Component {
    state = {};

    constructor(props) {
        super(props);
    }

    componentDidMount() {
        const canvas = document.getElementById('top');
    }

    render() {
        return ('');
    }
}

export default HorizontalRenderer;